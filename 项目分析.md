# 纪念币快速预约助手 - 功能与实现原理分析

## 一、项目概述

这是一个 **Chrome 浏览器扩展程序**，用于自动化填写中国各大银行（交通、中行、农行、工商、建行、华夏、储蓄、苏州银行）的纪念币预约表单，帮助用户快速完成预约流程。

## 二、核心功能

### 1. **个人信息管理**
- 添加、编辑、删除多个用户信息
- 支持字段：姓名、身份证号、手机号、预约网点、预约数量
- 数据验证（身份证15/18位、手机号11位）
- 防重复检测（身份证号和手机号唯一性校验）

### 2. **一键填充表单**
- 智能识别银行网站的表单元素
- 自动填充个人信息到对应输入框
- 支持输入框（input）和下拉选择框（select）
- 触发表单验证事件确保数据生效

### 3. **数据导入/导出**
- JSON 格式导出个人信息
- 支持批量导入备份数据
- 数据持久化存储

### 4. **快速导航**
- 内置8家银行预约链接
- 可折叠的链接面板
- 一键跳转到预约页面

## 三、技术架构

### 3.1 架构设计

```
┌─────────────────────────────────────────┐
│         Chrome Extension                │
├─────────────────────────────────────────┤
│                                         │
│  ┌──────────────┐   ┌───────────────┐  │
│  │ Background   │   │   Popup UI    │  │
│  │  (管理窗口)   │◄─►│  (主界面)      │  │
│  └──────────────┘   └───────┬───────┘  │
│                             │          │
│                     ┌───────▼───────┐  │
│                     │   Edit UI     │  │
│                     │  (编辑窗口)    │  │
│                     └───────────────┘  │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │      Content Script             │   │
│  │   (注入到银行网页)               │   │
│  └─────────────────────────────────┘   │
│              ▲                          │
└──────────────┼──────────────────────────┘
               │
    ┌──────────▼────────────┐
    │   Bank Website        │
    │   (银行预约网站)       │
    └───────────────────────┘
```

### 3.2 文件结构

| 文件 | 作用 | 代码行数 |
|------|------|---------|
| **manifest.json** | 扩展配置文件（Manifest V3） | 32 |
| **background.js** | 后台服务，管理窗口生命周期 | 30 |
| **popup.js** | 主界面逻辑，数据管理 | 241 |
| **popup.html/css** | 主界面UI | - |
| **content.js** | 内容脚本，表单填充核心 | 125 |
| **edit.js** | 编辑对话框逻辑 | 108 |
| **edit.html/css** | 编辑界面UI | - |

### 3.3 目录结构

```
/home/user/jnb/
├── .git/                           (git 版本控制)
├── 项目分析.md                     (本文档)
└── 纪念币预约助手v2/
    ├── jnb/                        (主要扩展代码)
    │   ├── manifest.json           (扩展配置)
    │   ├── background.js           (后台脚本)
    │   ├── popup.html/js/css       (主界面)
    │   ├── edit.html/js/css        (编辑界面)
    │   ├── content.js              (内容脚本)
    │   └── icon.png                (扩展图标)
    └── 预约测试/                    (测试页面和资源)
        ├── 中国银行纪念币销售.html
        ├── 建设银行纪念币模拟页面.html
        ├── 纪念币预售首页_中国农业银行.html
        └── 纪念币预约工商.html
```

## 四、核心实现原理

### 4.1 消息通信机制

使用 **Chrome Extension Message Passing** 实现组件间通信：

```javascript
// popup.js → content.js
chrome.tabs.sendMessage(tabId, {
  action: 'fillPersonalInfo',
  data: info
}, callback);

// content.js 接收消息
chrome.runtime.onMessage.addListener((request, sender, sendResponse) => {
  if (request.action === 'fillPersonalInfo') {
    // 执行填充逻辑
    sendResponse({ success: true });
  }
});
```

### 4.2 智能表单识别（content.js:10-65）

**核心创新：多选择器匹配策略**

为每个字段定义了多个可能的选择器，按优先级匹配：

```javascript
const selectors = {
  name: [
    '#USR_NM',                    // 建行专用
    '#txtName',                   // 通用命名
    'input[name*="name" i]',      // 模糊匹配（不区分大小写）
    'input[placeholder*="姓名"]',  // 占位符匹配
    '#oppAcNme',                  // 特定银行
    // ... 20+ 种可能的选择器
  ],
  idCard: [/* 15+ 种选择器 */],
  phone: [/* 18+ 种选择器 */],
  // ...
};
```

**匹配逻辑**（content.js:68-89）：
1. 遍历每个字段的所有选择器
2. 使用 `document.querySelector()` 查找元素
3. 找到第一个匹配的元素即停止
4. 根据元素类型（input/select）采用不同填充策略

### 4.3 表单填充技术

#### 输入框填充（content.js:79-84）
```javascript
element.value = value;
// 触发多种事件确保框架（React/Vue）能检测到变化
['input', 'change', 'blur', 'focus'].forEach(eventType => {
  const event = new Event(eventType, { bubbles: true });
  element.dispatchEvent(event);
});
```

**为什么要触发这些事件？**
- `input`: 实时输入事件，框架通常监听此事件
- `change`: 值改变事件，触发表单验证
- `blur`: 失焦事件，激活验证规则
- `focus`: 聚焦事件，确保元素交互状态

#### 下拉框填充（content.js:96-124）
```javascript
function handleSelect(select, value) {
  // 1. 完全匹配
  for (let option of select.options) {
    if (option.value === value || option.text === value) {
      select.selectedIndex = i;
      found = true;
    }
  }

  // 2. 模糊匹配（容错处理）
  if (!found) {
    for (let option of select.options) {
      if (option.text.includes(value)) {
        select.selectedIndex = i;
      }
    }
  }

  // 3. 触发 change 事件
  select.dispatchEvent(new Event('change', { bubbles: true }));
}
```

### 4.4 数据持久化（Chrome Storage API）

**存储结构**：
```javascript
{
  infoList: [
    {
      userName: "张三",
      idCard: "110101199001011234",
      phone: "13800138000",
      appointmentBranch: "北京分行",
      appointmentQuantity: "2"
    },
    // ...
  ],
  autoVerifyCode: false,
  autoFill: true,
  bankLinksExpanded: false
}
```

**操作示例**（popup.js:71-136）：
```javascript
// 读取
chrome.storage.local.get('infoList', (result) => {
  const list = result.infoList || [];
});

// 写入
chrome.storage.local.set({ infoList: list }, callback);
```

### 4.5 窗口管理（background.js）

**防止重复打开**（background.js:2-29）：
```javascript
chrome.action.onClicked.addListener(() => {
  chrome.windows.getAll({ populate: true }, (windows) => {
    // 检查是否已有 popup 窗口
    const existing = windows.find(w =>
      w.type === 'popup' &&
      w.tabs[0]?.url?.includes('popup.html')
    );

    if (existing) {
      // 聚焦现有窗口
      chrome.windows.update(existing.id, { focused: true });
    } else {
      // 创建新窗口
      chrome.windows.create({
        url: 'popup.html',
        type: 'popup',
        width: 250,
        height: 350
      });
    }
  });
});
```

### 4.6 数据验证（edit.js:77-96）

**身份证校验**：
```javascript
const idCardReg = /(^\d{15}$)|(^\d{18}$)|(^\d{17}(\d|X|x)$)/;
// 支持：15位、18位数字、17位数字+X/x
```

**手机号校验**：
```javascript
const phoneReg = /^1[3-9]\d{9}$/;
// 1开头，第二位3-9，共11位
```

**防重复**（edit.js:41-49）：
```javascript
const isDuplicate = list.some((item, idx) => {
  if (mode === 'edit' && idx === parseInt(index)) return false;
  return item.idCard === info.idCard || item.phone === info.phone;
});
```

### 4.7 数据导入/导出（popup.js:199-242）

**导出**：
```javascript
function exportData() {
  chrome.storage.local.get('infoList', (result) => {
    const data = JSON.stringify(result.infoList || []);
    const blob = new Blob([data], { type: 'application/json' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = '纪念币快速预约.json';
    a.click();
    URL.revokeObjectURL(url);  // 释放内存
  });
}
```

**导入**：
```javascript
function importData() {
  const input = document.createElement('input');
  input.type = 'file';
  input.accept = '.json';

  input.onchange = (e) => {
    const file = e.target.files[0];
    const reader = new FileReader();

    reader.onload = (event) => {
      try {
        const data = JSON.parse(event.target.result);
        if (Array.isArray(data)) {
          chrome.storage.local.set({ infoList: data }, callback);
        }
      } catch (err) {
        showNotification('数据格式错误！', 'error');
      }
    };

    reader.readAsText(file);
  };

  input.click();
}
```

## 五、数据流转图

```
┌──────────────┐
│   用户操作    │
└──────┬───────┘
       │
       ▼
┌──────────────────────┐
│  Popup UI            │
│  - 点击"填写"按钮      │
└──────┬───────────────┘
       │
       │ chrome.tabs.sendMessage()
       │ { action: 'fillPersonalInfo', data: info }
       ▼
┌──────────────────────────────┐
│  Content Script              │
│  1. 接收消息                  │
│  2. 遍历选择器列表            │
│  3. 查找表单元素              │
│  4. 填充数据                  │
│  5. 触发事件                  │
└──────┬───────────────────────┘
       │
       │ sendResponse({ success: true })
       ▼
┌──────────────────────┐
│  Popup UI            │
│  - 显示成功通知       │
└──────────────────────┘
```

## 六、关键技术点总结

### 6.1 兼容性设计
- **多选择器策略**：覆盖不同银行的表单元素命名习惯
- **事件触发机制**：兼容原生表单和现代框架（React/Vue）
- **模糊匹配**：下拉框选项的容错处理

### 6.2 用户体验优化
- **防重复打开**：避免多个窗口混乱
- **实时保存**：操作后立即持久化
- **通知反馈**：操作结果即时提示
- **数据校验**：前端验证减少错误

### 6.3 Manifest V3 特性
- 使用 `chrome.scripting` 替代旧的 `chrome.tabs.executeScript`
- `host_permissions` 替代 `permissions` 中的 URL 权限
- Service Worker 替代 Background Page（本项目使用持久化脚本）

### 6.4 安全考虑
- **权限最小化原则**：只请求必要的权限（storage, activeTab, scripting）
- **数据本地化**：所有数据存储在本地，不上传服务器
- **输入验证**：防止无效数据存储

## 七、支持的银行列表

| 银行名称 | 预约链接 |
|---------|---------|
| 交通银行 | https://apply.95559.com.cn/personbank/commemorativeOrderCoins/index.html#/order |
| 中国银行 | https://cmcoins.boc.cn/BOC15_CoinSeller/welcome.html |
| 农业银行 | https://eapply.abchina.com/coin/coin/CoinIssuesDistribution?typeid=202307 |
| 工商银行 | https://static.jnb.icbc.com.cn/ICBC/ICBCCOIN/roccentryPC.html |
| 建设银行 | https://ccb.com/cn/jnb/subject/index.html |
| 华夏银行 | https://mcm.hxb.com.cn/p/coin/coinHomeWeb.html?publish_channel_id=006 |
| 储蓄银行 | https://pbank.psbc.com/perbank/commemorativeCoinMake.gate |
| 苏州银行 | https://upbp.startbank.com.cn/rvccPC/JNB/index.html?channel=07# |

## 八、潜在的改进方向

1. **验证码识别**：目前只有开关，未实现自动识别功能
2. **智能等待**：表单元素延迟加载时的重试机制
3. **批量预约**：支持多人信息一键批量填写
4. **日志记录**：记录填写历史和成功率
5. **云同步**：可选的跨设备数据同步功能
6. **表单检测**：主动识别当前页面是否为预约页面
7. **定时提醒**：预约开放时间提醒功能
8. **成功率统计**：分析各银行预约成功率

## 九、使用说明

### 安装方法
1. 打开 Chrome 浏览器，进入 `chrome://extensions/`
2. 开启右上角"开发者模式"
3. 点击"加载已解压的扩展程序"
4. 选择 `纪念币预约助手v2/jnb` 文件夹
5. 扩展安装成功，工具栏会显示图标

### 使用步骤
1. **添加个人信息**：点击扩展图标，点击"+"添加个人信息
2. **填写表单**：进入银行预约页面，点击个人信息卡片的"填写"按钮
3. **完成预约**：检查自动填充的信息，提交预约

### 数据备份
- **导出**：点击"导出"按钮，保存 JSON 文件
- **导入**：点击"导入"按钮，选择之前导出的 JSON 文件

## 十、总结

这个扩展程序的核心价值在于：
- ✅ **智能识别**：通过多选择器策略适配不同银行网站
- ✅ **自动化**：减少手动输入，提高预约效率
- ✅ **易用性**：简洁的界面，直观的操作流程
- ✅ **可靠性**：数据验证、防重复、事件触发等机制确保成功率

实现原理上采用了标准的 Chrome Extension 架构，通过 Content Script 注入和消息传递实现跨页面通信，使用 Storage API 实现数据持久化，整体设计合理且实用。

---

**文档版本**: v1.0
**生成日期**: 2026-01-13
**分析工具**: Claude Code
